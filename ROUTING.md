# サブエージェントのモデルルーティング（動的ルーティング規範）

サブエージェントを起動する全ての場面で、以下に従う。目的はクレジット効率: 安いモデルから始め、品質条件を満たせないときだけ昇格する。

## 体制（2026-09-05 改定）

- **親（オーケストレーター）= gpt-6-astra**（config.toml の既定モデル）。親は設計・分解・統合・最終判断を担い、実装・調査・検収は子へ委譲する
- **子は gpt-5.6-terra と gpt-5.6-sol の2モデルのみ**。子に gpt-6-astra を指定しない（親と同格の子を作ると分担の意味が消える）
- 子の config 既定床は terra/high（config.toml `[agents]`）。ただし床への依存は禁止で、毎回モデルと推論強度を明示指定する（運用ルール1）
- モデルと推論強度は別々に選ぶ。まずタスク種別でモデルを決め、次に下の基準で推論強度を決める

## ルーティング表（モデル）

| タスク種別 | モデル / 推論強度の既定 |
|---|---|
| 軽量・定型作業（整形、単純検索、機械的変換） | gpt-5.6-terra / medium |
| 調査・実装（通常の開発作業） | gpt-5.6-terra / high |
| 難所、または一度失敗したタスクの再試行 | gpt-5.6-sol / high へ昇格 |
| 高リスクな設計判断・最終統合・最終レビュー | gpt-5.6-sol / ultra（親が自分で担う場合は親のまま） |

## 推論強度の選択基準（effort は表の既定から動的に上下させる）

| 推論強度 | 選ぶ条件 |
|---|---|
| medium | 手順が既知で、出力の正誤が機械的に確認できる（テスト・diff・grep で判定可） |
| high | 既定。調査・実装・検収の通常作業 |
| xhigh | 複数ファイル間の因果追跡、再現条件を特定できていない不具合の切り分け、または high で未解決となった検収項目の再検証に用いる。routing 宣言には該当条件と未解決事項を記す。ただし、ルール6(b)に該当する再検証はルール8の sol/high 指定を優先し、xhigh の先行を要求しない |
| ultra | sol 限定。通常の昇格段階に含めず、運用ルール7の条件（単発の高リスク設計判断、または実際の最終ゲート）を必須とする |

- FAIL とは、固定した受入基準を成果物が満たさないことを証跡付きで確認した状態を指す（検収自体の誤りは FAIL に含めない）
- 検収報告の FAIL に再現手順（実行したコマンドまたは確認手順と、その出力）が無い場合、それは成果物の FAIL ではなく検収の不備として扱う。この場合は実装側の再試行を起動せず、同じモデル・同じ推論強度で検収だけを1回やり直す。この検収のやり直しは、既存の失敗イベントにおけるルール6(b)の Sol 利用枠および再検証回数に算入しない。再検収でも再現手順が得られなければ「検証不能」として扱い、ルール8に従い完了扱いにしない。この判定を行えるのは親だけで、実装担当が検収結果を否認することはできない
- effort の昇格は medium→high→xhigh とし、terra では xhigh を上限とする。モデル昇格（terra→sol）は、先行再試行後も同じ受入基準が未達である証跡が得られた場合に判断する。両方を同時に上げるのは、運用ルール6の失敗イベントで対象と証跡を特定した時だけ
- 降格は運用ルール6・7に従う。effort も同様で、同種タスクが xhigh で2回連続 PASS したら次回から high に戻す
- 宣言形式は変えない: `routing: <タスク概要> → <モデル>/<effort> — <一文の理由>`。理由欄にはモデルの選定理由と effort の選定理由を両方1文で書く

## 運用ルール

1. サブエージェント起動時は必ずモデルと推論強度を明示指定する。未指定（親設定の暗黙継承）での起動は禁止
2. 起動の直前に、次の形式の routing 宣言を応答本文に1行出力する（起動1件につき1行、省略禁止）:
   `routing: <タスク概要> → <モデル>/<effort> — <一文の理由>`
3. 最初から Sol を選べるのは表の昇格条件に該当する時だけ。該当理由を宣言の理由欄に書く
4. 全会話履歴の引き継ぎが必要でモデル変更ができない場合は、その制約自体を宣言の理由に書く
5. 並列数は必要最小限にし、十分な結果が出た時点で残りを打ち切る
6. **昇格は粘着させない（降格ルール）**: 失敗による Sol への昇格は、対象成果物・失敗した検収項目・失敗証跡を routing 宣言で特定した1件の失敗イベントにつき、(a) terra での effort 先行再試行の後に行う Sol での初回再試行と、(b) その出力に対する Sol での再検証1回に限る。terra での再試行はこの Sol 利用枠を消費しない。同じ未解決 FAIL について Sol 利用枠を再付与しない。同じ未解決FAIL、タスク名・担当エージェント・サイクル番号の変更は新しい失敗イベントとして扱わない。Sol 利用枠を使い切っても FAIL が解消しない場合は、その時点で作業を打ち切り、未解決の検収項目・最後の失敗証跡・試した経路（モデル/推論強度の列）をユーザーへ報告する。親が自分で実装や検収を肩代わりして完了扱いにすることはしない。この範囲を終えた後は、表の最小の該当ルートへ戻す。新たな昇格は、別の検収項目のFAIL、または成果物変更によるPASS済み項目の回帰が具体的に確認された場合に限る
7. **ultra の反復利用を制限する**: `gpt-5.6-sol / ultra`を選択できるのは、(a) routing 宣言で判断対象と具体的な高リスク要因を特定した単発の設計判断、または (b) 固定した成果物バージョンについて公開・入稿・納品の可否を決定する実際の最終ゲート（freeze確定・入稿前監査・最終統合・納品直前レビュー）に限る。最終ゲートに先立つ反復QAは最終ゲートに含めない。「同種タスク」とは対象成果物・受入基準・検証経路が実質的に同じタスクをいい、名称・プロンプト・担当エージェント・サイクル番号の変更では別種にならない。最終ゲート以外で同種タスクが `gpt-5.6-sol / ultra` で2回連続PASSした場合、次回から表の最小の該当ルート（上限 `gpt-5.6-sol / high`）へ降格する。ultraへの復帰は、6の新しい失敗イベント、判断対象または受入基準の実質変更、あるいは実際の最終ゲートが発生した場合に限る
8. **検収は実装担当とは別の新規エージェントに担わせる**: 実装・生成・設定変更についてユーザーへ成果物が完成・利用可能と伝える最終応答の前に、実装を担当していない検収専用サブエージェントを1本起動する。`spawn_agent` がモデル指定引数を公開するランタイムでは `spawn_agent(task_name="verify_<英小文字・数字・アンダースコアの識別子>", fork_turns="none", model="<表および6・7で決めたモデル>", reasoning_effort="<表および6・7で決めた推論強度>", message="<自己完結した検収依頼>")` とする。`message` には成果物の絶対パス、検収スコープと非目標、受入基準、実行環境、検証経路・コマンド、期待する報告形式（項目別PASS/FAIL・根拠・FAILの再現手順）を含め、実装者の合格主張は渡さない。全項目PASSを受け取った場合だけ完了報告し、FAILまたは検証不能なら完了扱いにしない。FAIL時は対象成果物・失敗項目・証跡を次の再試行のrouting宣言で特定し、6の失敗イベントとして扱う。検収モデルは表および6・7に従い、通常は `gpt-5.6-terra / high`、6(b)の再検証は `gpt-5.6-sol / high`、`ultra` は7の条件を満たす実際の最終ゲートに限る。`spawn_agent` が `model` / `reasoning_effort` を公開しないランタイムでは、1の例外として両引数を省略し、その制約とホスト既定値の使用をrouting宣言に明記する。省略できるのは、成果物の挙動・契約・権限・データ・依存関係に影響しない誤字・表記・整形だけの局所変更、またはユーザーが検収不要と明示した場合に限り、単に1行であることは省略理由にしない。
